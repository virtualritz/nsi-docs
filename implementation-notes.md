# Shared Boundary Proposal

- Added an explanation draft comparing structural encodings for shared boundary identity.
- Scope correction: excluded parameter maps, displacement policy, and welding algorithms at the user's request.
- Recommended shared edge identities with explicit local uses; selector encoding and instance scope remain open.
- Kept existing alternative drafts intact. Added the new page to the book summary.
- Validation: `just book` passed. It reported an mdbook-admonish/mdbook version mismatch warning. `git diff --check` passed.
- Named the proposed nodes `weld` and `weld-use` to state their purpose; updated connection names and examples consistently.
